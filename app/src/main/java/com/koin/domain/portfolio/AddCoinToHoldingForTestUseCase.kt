package com.koin.domain.portfolio

import javax.inject.Inject

//class AddCoinToHoldingForTestUseCase @Inject constructor(
//    private val portfolioRepository: PortfolioRepository
//) {
//    suspend operator fun invoke(coinId: String, quantity: Double, pricePerCoin: Double): Result<Unit> {
//        return portfolioRepository.addCoinToHoldingForTest(coinId, quantity, pricePerCoin)
//    }
//}
